# ==========================================================================
# Orkai — Tauri 2 (Rust) + React/TypeScript (Vite) monorepo, Windows-first.
# Lockfiles (Cargo.lock, package-lock.json) SÃO versionados: é um app, não lib.
# ==========================================================================

# --- Rust / Cargo -----------------------------------------------------------
target/
**/*.rs.bk
*.pdb

# --- Node / npm -------------------------------------------------------------
node_modules/
.npm/
*.tsbuildinfo

# --- Build output (frontend) ------------------------------------------------
dist/
dist-ssr/
.vite/

# --- Tauri ------------------------------------------------------------------
apps/desktop/src-tauri/gen/
# Bundles gerados (instaladores). Vão para GitHub Releases, não pro repo.
apps/desktop/src-tauri/target/
*.msi
*.exe
*.nsis
*.AppImage
*.dmg
*.deb

# --- Banco de dados local ---------------------------------------------------
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# --- Segredos e ambiente ----------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key

# --- Logs e relatórios ------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage/
.nyc_output/

# --- Editores / IDEs --------------------------------------------------------
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
*~

# --- Sistema operacional ----------------------------------------------------
# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
# macOS (contribuidores em outra plataforma)
.DS_Store
.AppleDouble
._*
# Linux
.directory
.Trash-*

# --- Diversos ---------------------------------------------------------------
*.bak
*.tmp
tmp/
